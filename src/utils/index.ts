export const splitName = (name: string): string => {
    return name.split(':')[1];
}

export const formatCurrency = (amount: number, currency: string = 'us'): string => {
    return amount + '';
}

export const averagePrices = (prices: number[]): number => {
    return 0;
}